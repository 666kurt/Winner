import SwiftUI

struct TrainingScreen: View {
    
    var body: some View {
        VStack {
            Text("TrainingScreen")
                .font(.title)
                .foregroundColor(.white)
        }
        .backgroundModifier()
    }
    
}

#Preview {
    TrainingScreen()
}
